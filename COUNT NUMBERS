#include<bits/stdc++.h>
using namespace std;
int main()
{
	int T;
	cin >> T;
	while(T--)
	{
		string s;
		int i,n,count=0;
		cin >>n >> s;
		for(i=0 ; i < n ; i++)
		{
			if(isdigit(s[i]))
			{
				while(s[i] >= '0' && s[i] <= '9')
				  i++;
				  count++;
			}	
		}
		cout << count << endl;
	}
}
