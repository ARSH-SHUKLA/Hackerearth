#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n,i;
	cin >> n;
	long long int arr[n],total=0;
	for(i=0; i<n; i++)
	{
       cin >> arr[i];
	   total=total+arr[i];
	}
	cout << total;
}
